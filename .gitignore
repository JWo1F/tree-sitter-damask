# Keep the generated parser (src/) committed — Zed compiles src/parser.c and
# does not run `tree-sitter generate`. Regenerate with `tree-sitter generate`
# after editing grammar.js.
/bindings/
/build/
/node_modules/
Cargo.toml
Makefile
binding.gyp
*.wasm
